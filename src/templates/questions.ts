export function generateQuestionsTemplate(): string {
  return `# Pending Questions

<!-- TODO: Agent — as you analyze the codebase, add unresolved questions here. These are things you noticed but couldn't determine from the code alone — architecture decisions, missing docs, unclear ownership, etc. Remove questions as they get answered. -->
`;
}
